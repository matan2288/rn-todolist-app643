import { StyleSheet } from "react-native";
export const UpdateTodoScreenStyle = StyleSheet.create({
  MainContainer: {
    height: "100%",
    padding: 15,
    backgroundColor:
      "radial-gradient(circle, rgba(0,65,133,1) 0%, rgba(34,89,159,1) 100%)",
  },
});
